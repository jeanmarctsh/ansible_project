all:    

  children:

    workers:

      hosts:

        worker1:

          ansible_host: worker1
          ansible_user: worker1
        
        worker2:

          ansible_host: worker2
          ansible_user: worker2