---
- name: connectivity
  hosts: all
  gather_facts: true
  become: true

  tasks:
    - name: tester le connectivité des machines clientes
      ping:

    - debug:
        msg: " test de connectivé sur: {{ inventory_hostname }} avec comme nom DNS: {{ ansible_host }} " 